
version: "3.9"

services:
  website:
    image: nginx:latest
    container_name: my-website
    ports:
      - "80:80"
    volumes:
      - ./website:/usr/share/nginx/html
    restart: unless-stopped
